GET radius_top
GET height
GET radius_bottom
shape_volume = (
    1
    / 3
    * math.pi
    * height
    * (pow(radius_top, 2) + radius_top * radius_bottom + pow(radius_bottom, 2))
)
return shape_volume
